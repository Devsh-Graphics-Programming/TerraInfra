apiVersion: v1
kind: Namespace
metadata:
  name: apps-tools
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: kimai-mariadb-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: kimai-local
  hostPath:
    path: /mnt/data/mariadb
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kimai-mariadb-pvc
  namespace: apps-tools
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: kimai-local
  volumeName: kimai-mariadb-pv
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: kimai-var-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: kimai-local
  hostPath:
    path: /mnt/data/kimai-var
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kimai-var-pvc
  namespace: apps-tools
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: kimai-local
  volumeName: kimai-var-pv
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kimai
  namespace: apps-tools
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kimai-mariadb
  namespace: apps-tools
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kimai-mariadb
  namespace: apps-tools
  labels:
    app: kimai-mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kimai-mariadb
  template:
    metadata:
      labels:
        app: kimai-mariadb
    spec:
      serviceAccountName: kimai-mariadb
      initContainers:
        - name: fix-mariadb-perms
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 999:999 /var/lib/mysql"]
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
      containers:
        - name: mariadb
          image: mariadb:11
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            allowPrivilegeEscalation: false
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: mysql-root-password
            - name: MYSQL_DATABASE
              value: kimai
            - name: MYSQL_USER
              value: kimai
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: mysql-user-password
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mariadb-data
          persistentVolumeClaim:
            claimName: kimai-mariadb-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: kimai-mariadb
  namespace: apps-tools
spec:
  selector:
    app: kimai-mariadb
  ports:
    - port: 3306
      targetPort: 3306
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kimai
  namespace: apps-tools
  labels:
    app: kimai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kimai
  template:
    metadata:
      labels:
        app: kimai
    spec:
      serviceAccountName: kimai
      initContainers:
        - name: fix-kimai-perms
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 33:33 /opt/kimai/var"]
          volumeMounts:
            - name: kimai-var
              mountPath: /opt/kimai/var
        - name: ensure-db
          image: mariadb:11
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: mysql-root-password
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: mysql-user-password
          command:
            - bash
            - -c
            - |
              set -e
              for i in $(seq 1 30); do
                mariadb -h kimai-mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -e "\
                CREATE DATABASE IF NOT EXISTS kimai; \
                CREATE USER IF NOT EXISTS 'kimai'@'%' IDENTIFIED BY '$MYSQL_PASSWORD'; \
                ALTER USER 'kimai'@'%' IDENTIFIED BY '$MYSQL_PASSWORD'; \
                GRANT ALL PRIVILEGES ON kimai.* TO 'kimai'@'%'; \
                FLUSH PRIVILEGES;" && exit 0
                sleep 5
              done
              echo "Failed to prepare database" >&2
              exit 1
      containers:
        - name: kimai
          image: kimai/kimai2:apache
          securityContext:
            runAsNonRoot: true
            runAsUser: 33
            runAsGroup: 33
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8001
          env:
            - name: MAILER_URL
              valueFrom:
                secretKeyRef:
                  name: notification-smtp
                  key: MAILER_DSN
            - name: MAILER_DSN
              valueFrom:
                secretKeyRef:
                  name: notification-smtp
                  key: MAILER_DSN
            - name: MAILER_FROM
              valueFrom:
                secretKeyRef:
                  name: notification-smtp
                  key: MAILER_FROM
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: database-url
          volumeMounts:
            - name: kimai-var
              mountPath: /opt/kimai/var
      volumes:
        - name: kimai-var
          persistentVolumeClaim:
            claimName: kimai-var-pvc
---
apiVersion: batch/v1
kind: Job
metadata:
  name: kimai-bootstrap-admin-v4
  namespace: apps-tools
spec:
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      serviceAccountName: kimai
      restartPolicy: Never
      initContainers:
        - name: fix-kimai-perms
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 33:33 /opt/kimai/var"]
          volumeMounts:
            - name: kimai-var
              mountPath: /opt/kimai/var
      containers:
        - name: create-admin
          image: kimai/kimai2:apache
          securityContext:
            runAsNonRoot: true
            runAsUser: 33
            runAsGroup: 33
            allowPrivilegeEscalation: false
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: database-url
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kimai-db-credentials
                  key: mysql-user-password
            - name: KIMAI_ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: kimai-admin-credentials
                  key: username
            - name: KIMAI_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kimai-admin-credentials
                  key: password
            - name: KIMAI_ADMIN_EMAIL
              valueFrom:
                secretKeyRef:
                  name: kimai-admin-credentials
                  key: username
          volumeMounts:
            - name: kimai-var
              mountPath: /opt/kimai/var
          command:
            - bash
            - -c
            - |
              set -euo pipefail
              ADMIN_EMAIL="${KIMAI_ADMIN_EMAIL:-admin@devsh.eu}"
              if ! printf "%s" "$ADMIN_EMAIL" | grep -Eq '.+@.+\\..+'; then
                ADMIN_EMAIL="admin@devsh.eu"
              fi
              export KIMAI_ADMIN_EMAIL="$ADMIN_EMAIL"
              for i in $(seq 1 30); do
                php -r 'new PDO("mysql:host=kimai-mariadb;dbname=kimai","kimai",getenv("MYSQL_PASSWORD"));' && break
                sleep 5
              done
              php -r 'try {$pdo=new PDO("mysql:host=kimai-mariadb;dbname=kimai","kimai",getenv("MYSQL_PASSWORD")); $pdo->query("SELECT 1 FROM kimai2_configuration LIMIT 1"); exit(0);} catch (Throwable $e) {exit(1);}' || /opt/kimai/bin/console kimai:install -n
              if php -r '$pdo=new PDO("mysql:host=kimai-mariadb;dbname=kimai","kimai",getenv("MYSQL_PASSWORD")); $stmt=$pdo->prepare("SELECT 1 FROM kimai2_users WHERE username=? OR email=?"); $stmt->execute([getenv("KIMAI_ADMIN_USERNAME"), getenv("KIMAI_ADMIN_EMAIL")]); exit($stmt->fetch() ? 0 : 1);'; then
                exit 0
              fi
              /opt/kimai/bin/console kimai:user:create "$KIMAI_ADMIN_USERNAME" "$ADMIN_EMAIL" ROLE_SUPER_ADMIN "$KIMAI_ADMIN_PASSWORD"
      volumes:
        - name: kimai-var
          persistentVolumeClaim:
            claimName: kimai-var-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: kimai-service
  namespace: apps-tools
spec:
  selector:
    app: kimai
  ports:
    - port: 80
      targetPort: 8001
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kimai-cert
  namespace: apps-tools
spec:
  secretName: kimai-tls
  dnsNames:
    - ${ENV_PREFIX}kimai2.${BASE_DOMAIN}
  issuerRef:
    name: letsencrypt-http
    kind: ClusterIssuer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kimai-ingress
  namespace: apps-tools
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ${ENV_PREFIX}kimai2.${BASE_DOMAIN}
      secretName: kimai-tls
  rules:
    - host: ${ENV_PREFIX}kimai2.${BASE_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kimai-service
                port:
                  number: 80
