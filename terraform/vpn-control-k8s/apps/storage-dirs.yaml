apiVersion: batch/v1
kind: Job
metadata:
  name: vpn-control-storage-dirs
  namespace: kube-system
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: mkdir
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              set -eu
              mkdir -p /host-data/local-path
              chmod 0777 /host-data/local-path
          volumeMounts:
            - name: host-data
              mountPath: /host-data
      volumes:
        - name: host-data
          hostPath:
            path: /mnt/data
            type: Directory

